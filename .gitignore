# Virtual environment
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# Data (raw is large, keep processed for reproducibility)
data/raw/

# Model artifacts (default ignore, but V5 What-If bundles are committed for Docker demo)
*.joblib
*.pkl
*.pt
*.pth
app/models/*.joblib
!app/models/v5/*.joblib

# Environment
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Logs
*.log

# Timestamped backup snapshots produced by patch / regen scripts
*.backup_[0-9]*.csv

# Docker
docker/.env

# Optuna study storage (generated, regenerable from scripts/v5_tune_stage1.py)
optuna_v5.db
optuna_v5.db-journal

# LaTeX auxiliary files (only commit .tex / .bib / .pdf)
*.aux
*.bbl
*.blg
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz

# Large eski feature snapshot (15MB, not used by V5 pipeline)
data/processed/btc_features_all.csv

# Pre-overnight screenshots (paper figure source images live in docs/paper/figures/)
Screenshot*.png
